name: Build Violet Kernel (Method 3)
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 1. Clone Source
        run: |
          git clone --depth=1 https://github.com/Joker-V2/kernel_xiaomi_violet.git kernel

      - name: 2. Install Build Essentials
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential flex bison libssl-dev libelf-dev clang lld llvm zip curl wget gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi

      - name: 3. Setup KernelSU Next
        working-directory: kernel
        run: |
          git clone https://github.com/rifsxd/KernelSU-Next kernelsu_repo
          cp -r kernelsu_repo/kernel drivers/kernelsu
          echo "obj-y += kernelsu/" >> drivers/Makefile
          # Force Enable KSU in defconfig
          echo "CONFIG_KSU=y" >> arch/arm64/configs/violet_user_defconfig
          echo "CONFIG_KSU_NEXT=y" >> arch/arm64/configs/violet_user_defconfig

      - name: 4. Set Toolchain (AOSP Clang)
        run: |
          git clone --depth=1 https://github.com/kdrag0n/proton-clang clang

      - name: 5. Build Kernel (Strict Non-Interactive)
        working-directory: kernel
        run: |
          export PATH=$(pwd)/../clang/bin:$PATH
          export ARCH=arm64
          export SUBARCH=arm64
          export KBUILD_BUILD_USER="Gemini"
          export KBUILD_BUILD_HOST="Android-Builder"
          
          # Sabse zaroori step: Sawaal-jawab ko bypass karna
          make O=out violet_user_defconfig
          
          # Ye command saare naye options ko default par set karke aage badh jayegi
          yes "" | make O=out oldconfig || make O=out olddefconfig
          
          # Actual Compilation
          make -j$(nproc --all) O=out \
                      CC=clang \
                      CROSS_COMPILE=aarch64-linux-gnu- \
                      CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
                      AR=llvm-ar \
                      NM=llvm-nm \
                      OBJCOPY=llvm-objcopy \
                      OBJDUMP=llvm-objdump \
                      STRIP=llvm-strip \
                      KCONFIG_NOSILENTUPDATE=1

      - name: 6. Packaging AnyKernel3
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3 AnyKernel3
          # Compiled Image check
          if [ -f kernel/out/arch/arm64/boot/Image.gz-dtb ]; then
            cp kernel/out/arch/arm64/boot/Image.gz-dtb AnyKernel3/
          elif [ -f kernel/out/arch/arm64/boot/Image.gz ]; then
            cp kernel/out/arch/arm64/boot/Image.gz AnyKernel3/
          else
            cp kernel/out/arch/arm64/boot/Image AnyKernel3/
          fi
          cd AnyKernel3
          zip -r9 ../Violet-KSU-Kernel.zip *

      - name: 7. Upload Result
        uses: actions/upload-artifact@v4
        with:
          name: Violet-Kernel
          path: Violet-KSU-Kernel.zip
          
